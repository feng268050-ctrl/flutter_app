<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 按下状态 -->
    <item android:drawable="@mipmap/safety_botton" android:state_pressed="true"/>
    <!-- 禁用状态 -->
    <item android:drawable="@mipmap/safety_botton" android:state_enabled="false"/>
    <!-- 默认状态（兜底） -->
    <item android:drawable="@mipmap/safety_botton"/>
</selector>