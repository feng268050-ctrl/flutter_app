# Appliance NTP server preference (Settings Automatic is on by default).
# Primary: pool.ntp.org; then Cloudflare → Google → Aliyun as fallbacks.
[Time]
NTP=pool.ntp.org
FallbackNTP=time.cloudflare.com time.google.com ntp.aliyun.com
