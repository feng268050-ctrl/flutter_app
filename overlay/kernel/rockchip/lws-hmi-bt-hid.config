# Pin Bluetooth HID host support (Classic HIDP + BLE HOG/UHID).
# Verified present as =y on ynh960 baseline; keep explicit so trim fragments cannot drop them.
CONFIG_BT_HIDP=y
CONFIG_UHID=y
CONFIG_HID=y
CONFIG_HID_GENERIC=y
CONFIG_INPUT_EVDEV=y
