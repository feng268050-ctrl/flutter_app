[Unit]
# sim/virt motherboard: skip units that need RK356x GPT userdata, out-of-tree
# modules, or that stall early boot on this form-factor (same OS image).
ConditionKernelCommandLine=!lws.emulator=1
