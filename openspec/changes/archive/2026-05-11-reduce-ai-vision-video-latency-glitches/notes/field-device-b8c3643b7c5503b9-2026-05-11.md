# Field notes — device `b8c3643b7c5503b9` (2026-05-11)

Automated checks from the dev machine via `adb -s b8c3643b7c5503b9`. Use this file to fill evidence for `tasks.md` §0 / §1 / §6.

## Network facts (also covers parts of tasks 0.3 / 6.1)

| Check | Result |
|--------|--------|
| `eth0` state | UP (`LOWER_UP`), mtu 1500 |
| Tablet address | `192.168.1.10/24` |
| Route to camera subnet | `192.168.1.0/24 dev eth0 scope link src 192.168.1.10` |
| ICMP to default IPC `192.168.1.100` | 3/3 replies, ~0.9 ms RTT avg, 0% loss |

## RTSP URL for VLC comparison (tasks 0.1 / 1.1)

Use the **same** URL as the app (`CameraConfig.CAMERA_RTSP_MAIN_URL` or `LIVE_INFERENCE_RTSP_URL` for sub-stream tests). Transport: **TCP** (matches `Client.TRANSTYPE_TCP`).

**Interpretation:** If both VLC and AI Vision feel slow → prioritize IPC/network/stream settings. If only the app is slow → player stack / decode type / CPU (see `notes/rca-ip-camera-latency.md`).

## Task 0.2 — `ENABLE_LENS_GUARD_STARTUP` A/B (camera connected, device `b8c3643b7c5503b9`)

| Run | Build | Evidence | Stream / subjective |
|-----|--------|----------|----------------------|
| **A — OFF** | APK already on device before downgrade (`skip LensGuard init enable=false` at cold start) | `application: skip LensGuard init enable=false …` | `AiVisionFragment: Surface ready, opening rtsp://192.168.1.100/PR0`; `EasyPlayerClient: POST VIDEO_DISPLAYED!!!` ~**316 ms** after `Surface ready` (log delta 13:44:42.061 → 13:44:42.377); no `LensGuardModel` / `LensGuard-I420` load on that trace |
| **B — ON** | `./gradlew :app:assembleDebug -PENABLE_LENS_GUARD_STARTUP=true` + `adb install -r -d app-debug.apk` | `LensGuardManager: Engine started, handle=…`; **no** `skip LensGuard init`; `LensGuardModel` YOLO_PP_DIAG + `LensGuardManager: Check result` | Live policy tried **sub** `PR1` then **main** `PR0`; `AiVisionFragment: VIDEO_DISPLAYED decodeType=0 firstFrameMs=396 … profile=main candidate=2/2`; `LIVE_VIDEO_SIZE 1920x1088`; `AiVisionFragment: AI frame decimation skipped=71 minIntervalMs=200` |

**Conclusion (0.2):** With camera connected, **OFF** avoids native inference threads entirely at app start; **ON** runs engine + periodic checks and AI frame path (with decimation). Use this table to correlate any extra glitch/latency with **decodeType=0** (soft/PTS pacing path) plus CPU load from LensGuard, not solely “AI on/off” in isolation.

**Note:** Install used `adb install -r -d` because workspace `versionCode` was below the APK previously on the device. Reinstall your usual CI/release APK when done if you need a higher `versionCode` again.

## Task 6.1 — Field checklist (same session, camera `192.168.1.100`)

| Checklist item | Result |
|----------------|--------|
| `eth0` up | UP, `192.168.1.10/24` |
| Subnet match IPC | Same `/24` as `192.168.1.100`; kernel route `192.168.1.0/24 dev eth0 src 192.168.1.10` |
| ICMP to camera | `ping -c 2 192.168.1.100` → 0% loss, RTT ~0.74 ms |
| VLC / same RTSP (TCP) | Operational per **tasks 0.1 / 1.1** (same URL family as app); app default live URL `rtsp://192.168.1.100/PR0` |
| AI Vision first-frame (measured) | **ON build:** `firstFrameMs=396` from `AiVisionFragment` after sub→main fallback; `decodeType=0`; `LIVE_VIDEO_SIZE 1920x1088` |
| Target `< N ms` | Record product target separately; this capture documents **396 ms** for traceability |

## Log markers (tasks 1.2 / 6.1 first-frame)

After opening **Device monitor → AI Vision** (or start activity with `extra_initial_tab_index=4`):

```bash
adb -s b8c3643b7c5503b9 logcat -s AiVisionFragment:I
```

Look for `VIDEO_DISPLAYED decodeType=… firstFrameMs=…` and `LIVE_VIDEO_SIZE …`.

## Stress (task 6.2)

Manual: disconnect Ethernet briefly, reconnect; note time until stable preview and any sustained corruption. Target recovery bound **N** seconds should be filled by QA after this run.
