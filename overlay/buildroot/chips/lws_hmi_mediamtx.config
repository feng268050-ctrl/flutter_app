# Legacy marker — MediaMTX is no longer staged into rootfs-overlay.
# Product binary: prebuilt/mediamtx → /opt/hmi/bin/mediamtx via make build-app.
# Do not #include this fragment in the product defconfig.
