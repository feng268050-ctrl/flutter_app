# Generic OS overlay, then SoC-thin overlay (chip last so it can override).
BR2_ROOTFS_OVERLAY+="board/rockchip/common/rootfs-overlay/"
BR2_ROOTFS_OVERLAY+="board/rockchip/rk3566_rk3568/rootfs-overlay/"
