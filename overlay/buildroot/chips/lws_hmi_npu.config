# RKNPU2 runtime without demo binaries (§3.5).
# This SDK has no BR2_PACKAGE_RKNPU2 — stage prebuilt into lws-hmi-fs-overlay instead:
#   usr/lib/librknnrt.so
#   usr/bin/rknn_server
# Populate: make fetch-rknn-rt  (SDK external/rknpu2 preferred; github fallback)
