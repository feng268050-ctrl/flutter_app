# Seed system-wide proxy policy (D18). Applied by /usr/libexec/network/apply-proxy.sh
enabled=false
# http_proxy=http://host:8080
# https_proxy=http://host:8080
# ftp_proxy=
# all_proxy=socks5h://host:1080
no_proxy=localhost,127.0.0.1
