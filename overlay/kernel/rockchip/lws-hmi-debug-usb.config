# Optional kernel fragment (not in default ynh960_defconfig): USB gadget ECM for bench USB tethering.
# Default engineering access: UART2 / ttyFIQ0 serial console (see README make serial-console).
CONFIG_USB_CONFIGFS_ECM=y
CONFIG_USB_ETH=y
CONFIG_USB_CONFIGFS_F_FS=y
CONFIG_USB_FUNCTIONFS=y
