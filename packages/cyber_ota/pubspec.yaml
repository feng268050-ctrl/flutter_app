name: cyber_ota
description: >
  Reusable Dart OTA orchestration for LWS product HMIs — manifest fetch,
  version compare, cloud/host ingress, verify gate, and staged apply progress.
  Not a HAL module.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.5.0

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
