#!/usr/bin/env bash
# Retired: Innohi MainServer/ParamUpdate skip lived in patch-innohi-mainserver.sh.
# Owned linux-sdk already has the skip (or never copies those binaries).
echo "patch-30-rootfs.sh: retired (MainServer skip no longer applied)" >&2
exit 0
