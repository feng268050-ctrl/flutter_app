## 1. 入口约束与代码收敛

- [x] 1.1 盘点并收敛 AI 图片上传调用点，确保业务路径统一调用 `AiUploadCoordinator.enqueue`。
- [x] 1.2 对绕过队列的调用路径增加注释/防护（仅诊断用途，不作为产品行为）。

## 2. 可观测性与验证路径

- [x] 2.1 补充关键日志：enqueue、worker drain、上传成功、重试、清理，便于区分队列路径与外部直传。
- [x] 2.2 增加（或保留）最小调试触发入口，支持通过 App 队列执行上传验证。

## 3. 文档与回归口径

- [x] 3.1 更新 `upload.md`：明确“后续上传规范以 App WorkManager 队列为准；curl 仅用于网络诊断”。
- [x] 3.2 增加回归步骤：用 App 队列验证上传+清理+重试，不再将直传结果作为队列行为验收依据。
