# Apply USB Debug preference + VBUS plug-ssh when mode is peripheral.
# Prefer systemd oneshot (not long RUN under udevd).
ACTION=="add|change", SUBSYSTEM=="extcon", RUN+="/bin/systemctl start --no-block lws-hmi-usb-otg-role.service"
