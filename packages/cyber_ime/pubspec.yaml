name: cyber_ime
description: CyberIME — in-app overlay soft keyboard for LWS HMI (path package).
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cyber_ui:
    path: ../cyber_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
