name: cyber_pm
description: >
  Reusable Dart process supervisor for LWS product HMIs — spawn, stop,
  restart policies, and prefixed stdout/stderr log drain. Not a HAL module.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
