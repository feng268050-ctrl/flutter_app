# RKNN 联调回传模板（App -> YOLO）

## 1) 基础信息

- 设备型号：
- Android 版本：
- 内核版本：
- RKNPU 驱动版本：
- App 版本：
- 测试时间：

## 2) 本轮包信息

- libai.so 路径/版本：
- libai.so SHA256：
- librknnrt.so 版本与 SHA256：
- 模型命名（`cls/det` 或 `v8_*`）：
- 是否最小验证模式（`--init-only`）：
- I/O 模式（`IO_MEM` / `LEGACY_IO`）：

## 3) 运行结果

- 是否崩溃：
- 崩溃阶段最后日志标签（如 `RKNN_QUERY_OUTPUT_ATTR_END ret=0`）：
- 关键 ret code 列表（按时间顺序）：
- tombstone 路径：
- logcat 片段路径：

## 4) 样本与复现条件

- 输入样本 ID / 路径：
- 激光状态：
- 相机参数（曝光/增益/光照/fps）：
- 复现步骤（最短步骤）：
- 复现概率：

## 5) 结论与下一步建议

- App 侧结论：
- YOLO 侧需要补充项：
- 下一轮计划（A/B 切换、模型替换、日志增强等）：
