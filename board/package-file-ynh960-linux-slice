# ynh960 Linux pack (afptool requires parameter.txt — use exact Innohi extract at pack time).
# For manual DI slices only; normal install uses make flash (uf update.img).
# NAME	PATH
package-file	package-file
parameter	parameter.txt
uboot	uboot.img
boot	boot.img
vbmeta	vbmeta.img
dtbo	dtbo.img
baseparameter	baseparameter.img
super	rootfs.img
