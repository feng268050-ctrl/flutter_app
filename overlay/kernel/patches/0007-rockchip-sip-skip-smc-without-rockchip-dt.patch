From: lws-hmi kernel overlay
Subject: 0007-rockchip-sip-skip-smc-without-rockchip-dt.patch

diff -ruN a/drivers/firmware/rockchip_sip.c b/drivers/firmware/rockchip_sip.c
--- a/drivers/firmware/rockchip_sip.c
+++ b/drivers/firmware/rockchip_sip.c
@@ -14,6 +14,7 @@
 #include <linux/arm-smccc.h>
 #include <linux/io.h>
 #include <linux/module.h>
+#include <linux/of.h>
 #include <linux/rockchip/rockchip_sip.h>
 #include <asm/cputype.h>
 #ifdef CONFIG_ARM
@@ -34,12 +35,43 @@
 
 #define SIZE_PAGE(n)	((n) << 12)
 
+static bool rockchip_sip_el3_present(void)
+{
+	static int cached = -1;
+	struct device_node *root;
+	struct property *prop;
+	const char *compat;
+
+	if (likely(cached >= 0))
+		return cached;
+
+	cached = 0;
+	root = of_find_node_by_path("/");
+	if (!root)
+		return false;
+
+	of_property_for_each_string(root, "compatible", prop, compat) {
+		if (compat && !strncmp(compat, "rockchip,", 9)) {
+			cached = 1;
+			break;
+		}
+	}
+	of_node_put(root);
+	return cached;
+}
+
 static struct arm_smccc_res __invoke_sip_fn_smc(unsigned long function_id,
 						unsigned long arg0,
 						unsigned long arg1,
 						unsigned long arg2)
 {
 	struct arm_smccc_res res;
+
+	if (!rockchip_sip_el3_present()) {
+		res.a0 = SIP_RET_NOT_SUPPORTED;
+		res.a1 = res.a2 = res.a3 = 0;
+		return res;
+	}
 
 	arm_smccc_smc(function_id, arg0, arg1, arg2, 0, 0, 0, 0, &res);
 	return res;
