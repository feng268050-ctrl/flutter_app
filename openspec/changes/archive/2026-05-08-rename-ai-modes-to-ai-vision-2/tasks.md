## 1. 文案与资源统一

- [x] 1.1 在 Device detection 相关导航/标题中，将 `AI Modes` 文案统一替换为 `AI Vision`
- [x] 1.2 同步更新多语言资源（至少默认、中文、英文）并清理遗留 `AI Modes` 文案引用

## 2. 页面结构裁剪

- [x] 2.1 删除截图框选的顶部双开关区域（`AI Camera` / `AI Vision Training`）及其关联布局约束
- [x] 2.2 删除截图框选的右侧按钮区域（`Skip` / `Okay` / `Reselect`）及其关联事件与占位
- [x] 2.3 保留并整理左侧工作模式信息区字段展示（如 Detection Type / Work Mode / Recording Time）

## 3. 右侧实时视频流

- [x] 3.1 将右侧主画面区域绑定为实时视频流渲染视图，替换静态图占位
- [x] 3.2 实现视频流加载态/失败态提示与恢复显示逻辑
- [x] 3.3 确保应用构建包含 RTSP media source 依赖并验证点击 AI Vision 不崩溃

## 4. 视觉风格对齐

- [x] 4.1 按设备端基线统一 AI 区域样式（颜色语义、圆角、间距、信息层级）
- [x] 4.2 在目标分辨率与多语言场景下进行 UI 冒烟验证，确认无错位、裁切、黑屏和风格割裂
