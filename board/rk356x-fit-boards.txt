# SoC-family board inventory for multi-configuration boot FIT (platform W5).
# One board_id per line. FIT configuration name == board_id == OEM manifest board_id.
# DTB path after kernel build: arch/arm64/boot/dts/rockchip/<board_id>.dtb
# First line (non-comment) is the default configuration when present; otherwise ynh960.
# Do not list emulator/sim — P3.2 uses bare Image + QEMU virt DT.
ynh960
