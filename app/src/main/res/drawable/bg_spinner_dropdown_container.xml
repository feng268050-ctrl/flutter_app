<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 容器背景色：#FF060720（带透明度的深紫色） -->
    <solid android:color="#FF060720" />

    <!-- 边框：1dp 白色 -->
    <stroke
        android:width="1dp"
        android:color="#FFFFFF" />

    <!-- 圆角：10dp -->
    <corners android:radius="10dp" />

    <!-- 内边距：10dp（容器边缘与下拉项的间距） -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />

</shape>