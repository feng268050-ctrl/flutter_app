# ynh960: wall clock is the RK809/RK817 PMIC RTC (rk808-rtc). There is no
# external hym8563 on this board, so the PMIC RTC registers as rtc0. Rockchip
# defconfig still points HCTOSYS/SYSTOHC at rtc1 (EVB dual-RTC layout).
CONFIG_RTC_HCTOSYS_DEVICE="rtc0"
CONFIG_RTC_SYSTOHC_DEVICE="rtc0"
