--- a/drivers/rtc/rtc-rk808.c
+++ b/drivers/rtc/rtc-rk808.c
@@ -414,8 +414,6 @@
 	struct device_node *np;
 	int ret;
 
-	if(1)
-		return -EINVAL; 
 	switch (rk808->variant) {
 	case RK805_ID:
 	case RK808_ID:
