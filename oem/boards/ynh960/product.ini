# Factory seed for ynh960 OEM board (tunables only).
# Per-unit brand/model/sn live in Vendor Storage (make write-identity) — not here.
# oem-compose: fill blank runtime tunables only; identity keys ignored if present.
camera_ip=192.168.1.100
camera_type=1
