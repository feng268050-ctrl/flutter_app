# Emulator-only product identity stub (QEMU has no /dev/vendor_storage).
# read-product-identity.sh uses this only when Vendor Storage is absent.
# Not a production authority — real boards use make write-identity.
brand=LaserCyber
model=L1 Pro
sn=SIM-0001
