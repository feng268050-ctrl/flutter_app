package com.lasercyber.lws.ui.common.boot;

/**
 * Terminal and in-progress row states for the boot self-check dialog.
 */
public enum BootSelfCheckStatus {
    CHECKING,
    PASS,
    FAIL,
    SKIPPED
}
