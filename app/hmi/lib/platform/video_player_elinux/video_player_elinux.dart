/// Dart bindings for Sony eLinux GStreamer `libvideo_player_plugin.so`
/// (pigeon channel `dev.flutter.pigeon.VideoPlayerApi.*`).
///
/// Vendored from https://github.com/sony/flutter-elinux-plugins
/// `packages/video_player` — the pub package targets the `elinux` Flutter
/// platform; we register it manually on Wayland/Weston Linux embeds.
library;

export 'elinux_video_player.dart';
