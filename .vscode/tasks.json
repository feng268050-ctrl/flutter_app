{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "lws-hmi: debug setup",
      "type": "shell",
      "command": "make debug-host-prepare debug-setup build-debug-app",
      "options": {
        "cwd": "${workspaceFolder}",
        "envFile": "${workspaceFolder}/.env"
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      },
      "problemMatcher": []
    }
  ]
}
