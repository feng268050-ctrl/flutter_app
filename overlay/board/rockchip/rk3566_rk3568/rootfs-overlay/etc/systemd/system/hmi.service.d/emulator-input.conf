[Unit]
# Order after touch bridge on QEMU (that unit has ConditionKernelCommandLine=lws.emulator=1).
# Never add Condition= here — drop-in [Unit] merges into hmi.service and would skip HMI on hardware.
After=emulator-tablet-to-touch.service
