# ynh960 eth0: IC Plus IP101A on RMII (MDIO phy_id 0x02430c54).
# Without this, stmmac binds Generic PHY (irq=POLL) and skips IP101A APS /
# IRQ / phase setup — correlated with ~2.5 Mbps RTSP UDP + high RTP seq loss
# despite clean softnet/CRC counters.
CONFIG_ICPLUS_PHY=y
