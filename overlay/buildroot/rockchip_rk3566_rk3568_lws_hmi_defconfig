# lws-hmi Buildroot defconfig — product scope = uncommented #include lines below.
# Same make targets always; enable fragments in git as features land (P3/P5…).
# apply-overlay: gst/platform auto-switch to prebuilt overlays when exported.

#include "base/base.config"
#include "chips/lws_hmi_base.config"
#include "chips/lws_hmi_rootfs.config"
#include "chips/lws_hmi_systemd.config"
#include "chips/rk3566_rk3568_aarch64.config"
#include "gpu/gpu.config"
#include "wifibt/wireless.config"
#include "wifibt/bt.config"
#include "chips/lws_hmi_bt.config"
#include "chips/lws_hmi_network.config"
#include "chips/lws_hmi_font.config"
#include "powermanager.config"
# Default rootfs (make build-rootfs): Weston + eLinux + Mali wayland-gbm.
#include "chips/lws_hmi_wayland.config"
#include "chips/lws_hmi_flutter_weston.config"
# Alternate rootfs (make build-rootfs-flutter-pi): inject via LWS_HMI_WESTON=0.
# Switches to flutter-pi + Mali gbm; disables Wayland/Weston/eLinux.
# #include "chips/lws_hmi_flutter.config"
#include "chips/lws_hmi_p2_io.config"
# BR2_ROOTFS_OVERLAY for rootfs-overlay: see chips/rk3566_rk3568.config (apply-overlay)
# #include "fs/vfat.config"

# --- enable when feature is merged (not a separate make target) ---
#include "chips/lws_hmi_npu.config"
#include "chips/lws_hmi_gst_rtsp.config"
#include "chips/lws_hmi_mediamtx.config"
# Alarm Logs (sqlite3) + Modbus/YAML/Avahi — build or prebuilt via apply-overlay swap.
#include "chips/lws_hmi_platform.config"

#include "chips/lws_hmi_build.config"
#include "chips/lws_hmi_toolchain_external.config"
