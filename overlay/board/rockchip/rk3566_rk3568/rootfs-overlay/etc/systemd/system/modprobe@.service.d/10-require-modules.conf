[Unit]
# Multi-board: skip modprobe when this image has no /lib/modules tree
# (virt motherboard uses a mostly built-in kernel; empty modules dir makes
# systemd-modprobe@*.service stall under systemd-executor on QEMU).
ConditionDirectoryNotEmpty=/lib/modules
