name: cyber_alarm
description: >
  Shared warn/alarm episode engine for LWS product HMIs — catalog model,
  episode lifecycle, and ports. Apps wire Modbus/CyberUI adapters.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
